version: 2

build:
  image: latest

python:
  install: true
    - method: pip
      path: .
      extra_requirements:
        - docs
  version: 3.7
  extra_requirements:
    - dev

sphinx:
  builder: html
  fail_on_warning: true
